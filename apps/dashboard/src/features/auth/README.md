# Feature: Auth

Autenticação e perfil do usuário (profissional/gestor).

- **components/** — Componentes de login, seletor de perfil (quando extraídos).
- **hooks/** — Ex.: uso de auth já em `lib/auth-context`; hooks específicos aqui se necessário.
